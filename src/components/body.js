import './styles/styleBody.css'
// import { withStyles } from '@material-ui/core/styles';
import Button from '@material-ui/core/Button';

function Body() {
    return (
        <div className="createList">
            <Button variant="contained">Criar lista</Button>
        </div>
    );
}

export default Body;
